<div class="col-md-12">
  <div class="group">
    <%= link_to("提交简历", new_job_resume_path(@job), class: "btn btn-sm btn-primary")%>
  </div>

  <h2><%= @job.title %></h2>
  <p><%= @job.description %></p>

</div>
