<div class="col-md-4 col-md-offset-4">
  <h2><%= @job.title %>-"提交简历" </h2>
  <%= simple_form_for [@job, @resume] do |f| %>
    <%= f.input :content %>
    <%= f.input :attachment %>
    <%= f.submit "Submit", class: "btn btn-sm btn-primary"%>
  <% end %>
</div>
